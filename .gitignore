# dependencies
node_modules/

# secrets & credentials — NEVER commit these
.env
.env.local
.secrets/
*.enc

# WhatsApp session (contains an active login to your account)
.wwebjs_auth/
.wwebjs_cache/

# local / generated spreadsheets & Excel lock files
*.xlsx
~$*.xlsx

# logs
*.log
npm-debug.log*

# SQLite data files (local only)
*.sqlite
*.sqlite3

# local agent / skill tooling (not app code)
.agents/
.claude/
.codex/
skills-lock.json
